name: Merge Notification

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Slack notification
      if: github.event.pull_request.merged == true
      run: |
        curl -d '{"text": "${{ github.event.pull_request.user.login }} just merged ${{ github.head_ref }}!"}' -H "Content-Type: application/json" -X POST ${{secrets.SLACK_WEBHOOK}}
