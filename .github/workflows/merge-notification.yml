name: Merge Notifications

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Slack notification
      run: |
        curl -d '{"text": "$GITHUB_ACTOR released $GITHUB_REF!"}' -H "Content-Type: application/json" -X POST ${{secrets.SLACK_WEBHOOK}}
